{
  "manifest_version": 1,
  "id": "git",
  "name": "Git + GitHub CLI",
  "version": "0.16.1",
  "description": "Installs git and the GitHub CLI (gh) into the workspace, survives restarts, provides a settings panel for gh login (token stored in the zero-knowledge secret store), and serves the GitHub PR dashboard (open PRs for you and your team, plus repo status) the workspace nav reads.",
  "tier": "inprocess",
  "publisher": "TekFlox",
  "resource_estimate": {
    "cpu": "low",
    "memory": "low",
    "disk": "low"
  },
  "runtime": {
    "python": ">=3.11",
    "entrypoint": "git_app.plugin:GitAppPlugin",
    "pip_requires": [
      "httpx>=0.24"
    ]
  },
  "permissions": [
    "commands:install",
    "routes:register",
    "secrets:own",
    "fs:workspace-data",
    "net:outbound",
    "watchdog:tasks",
    "notifications:send"
  ],
  "contributes": {
    "routes": [
      {
        "prefix": "/api/apps/git"
      }
    ],
    "system_clis": [
      {
        "name": "git",
        "installer": "scripts/install_git.sh",
        "version": ">=2.30",
        "verify": "test -x \"$(git --exec-path)/git-remote-https\""
      },
      {
        "name": "gh",
        "installer": "scripts/install_gh.sh",
        "version": ">=2.40",
        "verify": "gh --version"
      }
    ],
    "windows": [
      {
        "id": "git.main",
        "title": "Git & GitHub CLI",
        "body": {
          "type": "declarative",
          "spec": "windows/main.json"
        }
      }
    ],
    "settings_panels": [
      {
        "id": "git.settings",
        "window": "git.main"
      }
    ]
  },
  "config_schema": {
    "type": "object",
    "properties": {
      "git_user_name": {
        "type": "string",
        "description": "git config --global user.name. Filled in automatically from your GitHub account when you sign in \u2014 set it here only to override."
      },
      "git_user_email": {
        "type": "string",
        "description": "git config --global user.email. Filled in automatically from your GitHub account when you sign in (a private email becomes the GitHub noreply address, which still authors pushes correctly)."
      },
      "watchdog_enabled": {
        "type": "boolean",
        "default": true,
        "description": "Watch the workspace's repos and notify (persistent notification) when a repo has uncommitted changes."
      },
      "watchdog_interval_s": {
        "type": "number",
        "default": 300,
        "description": "How often (seconds) the uncommitted-changes watchdog re-scans the workspace's repos. Minimum 30s."
      },
      "github_team": {
        "type": "string",
        "description": "PR dashboard: comma-separated GitHub logins whose open PRs are shown next to yours. Left empty, only your own PRs and the ones awaiting your review are listed."
      },
      "github_host": {
        "type": "string",
        "description": "PR dashboard: GitHub Enterprise host for the gh CLI (GH_HOST). Leave empty for github.com."
      },
      "github_poll_interval_s": {
        "type": "number",
        "default": 300,
        "description": "PR dashboard: how often (seconds) to re-poll GitHub for PRs and repo status. Minimum 60s."
      }
    },
    "required": []
  },
  "dependencies": {
    "apps": [
      {
        "id": "diff-tool",
        "version": ">=0.1.0",
        "required": true,
        "reason": "The repos nav's \"show diff\" arrow opens aw-app-diff-tool's window (POST /api/apps/diff-tool/diffs/render). See tekflox/aw-app-diff-tool README."
      }
    ]
  },
  "migrations": {}
}
